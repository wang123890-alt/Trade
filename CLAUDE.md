# 交易損益（Trade）App — 協作備忘

## 效率優先（重要，額度有限）

- **動手前先查現有資源，不重工**：使用者名下還有其他 repo（例如 `wang123890-alt/Choose`，一個台股篩選/分析工具），裡面可能已經踩過同樣的坑、驗證過同樣的資料來源或寫過類似邏輯。遇到資料來源、API串接、已知限制等問題時，**先查其他 repo 的程式碼或 `docs/` 底下的紀錄**，能直接引用結論就直接用，不要重新測試、重新踩雷、重新調研已經有答案的事。
- 使用者的方案額度有限、正在考慮是否續約，**浪費額度是實際成本**，不是抽象原則。每次操作前想一下「這步是必要的嗎，還是可以省略/合併」。
- 不要為了保險而重複驗證已經驗證過的東西（例如同一個 API 的 CORS 行為、同一段程式碼的既有邏輯）。

## 從無名稱/代號的K線截圖反查是哪支股票

- **全市場反查（用收盤價找股票）在這個環境做不到，不用每次重測**：`openapi.twse.com.tw` 被沙盒代理擋掉、TPEx OpenAPI 連不到、FinMind 免費版查全市場某天所有股票會被拒絕（免費版必須指定單一 `stock_id`），三條路都試過且都不通。
- **但 `wang123890-alt/Choose`（台股篩選工具）常常已經幫忙縮小範圍**：它的 `docs/validation/*.md` 驗證紀錄裡有多份「篩選結果清單」（例如「30日反彈型」「成交量集中族群」等），列出的是近期實際跑過篩選、真的有異動的個股代號。截圖裡的波段特徵（起漲低點、前波高點、漲幅型態）常常能跟這些清單對上。**收到沒有名稱/代號的K線截圖時，先看 Choose repo 這些清單裡有沒有型態相符的候選，再用 FinMind 逐一核對候選的 `TaiwanStockPrice`（單一 `stock_id` 免費版可查），比盲猜或跟使用者要求更多截圖更快。**
- 曾經漏掉的案例：截圖顯示波段起漲98.30、前波高點248、當日234跌0.64%，其實 Choose repo 驗證紀錄的「30日反彈型」清單裡已經列了「2351順德」，型態完全對得上，但仍花了一輪問使用者才確認——下次同類截圖要先比對清單。

## PR 流程

- 分支：`claude/trade-records-pnl-6cbsak`，對 repo `wang123890-alt/Trade`（public）。
- 每個功能改動開一個 PR（draft），完成後：
  - **無特殊需求或必要（無衝突、測試全過、無需要跟使用者確認的設計決策），直接合併，不用每次都問。**
  - 只有在真的有衝突、測試失敗、或設計上需要使用者決定時，才停下來問。
- 合併後固定流程：
  1. `git fetch origin main`
  2. 比對舊 commit 與新 `origin/main` 的 diff——如果只有 `data/store.json`（使用者手機/電腦透過 App 自動同步的交易資料）變動或完全無差異，代表可以安全對齊
  3. `git reset --soft origin/main`（絕不用 `--hard`，避免弄丟本地未推送的東西）
  4. `git push --force-with-lease origin claude/trade-records-pnl-6cbsak`
  5. 用 `curl` 輪詢 GitHub Pages 確認新內容已上線，再跟使用者說可以測試（瀏覽器快取可能落後，必要時提醒強制重新整理）
- main 分支偶爾會被 App 本身的 GitHub 同步功能直接寫入 `data/store.json`（"更新交易資料" commit，作者是使用者本人，透過手機/電腦的 App 觸發）——這是正常的資料同步，不是別人動了程式碼，diff 只會動到那個檔案。

## 測試

- 每次改動後跑 `node tests/*.test.js`（純 Node，無框架，逐檔執行），全部要過才能推送。
- `renderKLineChart`（`js/core/chart.js`）沒有專門的測試檔，改動後用臨時腳本（`node --input-type=module -e "..."`）驗證輸出的 SVG 字串。
- **K線SVG絕對不要同時給固定像素 `width`/`height` 屬性和 `viewBox`**：曾經寫成 `width="100%" height="${totalHeight}"`（`totalHeight` 是 viewBox 座標系的數字），這會讓瀏覽器渲染框的寬高比（容器寬 × 固定高）跟 viewBox 本身的寬高比不一致，`preserveAspectRatio` 預設會等比縮放置中，導致窄螢幕上下留下大片空白（使用者截圖抓到過這個問題）。正確做法：只設 `viewBox`，`style` 給 `width:100%; height:auto;`，讓高度依 viewBox 比例自動算。

## 安全

- **絕對不要**接受、記錄、或使用使用者在對話中貼的 GitHub PAT 或其他密鑰。若使用者不慎貼出，立刻提醒撤銷/重新產生，並說明新的 token 只能直接輸入到 App 本身的 Settings 頁面（裝置端），不能再透過對話傳遞。

## 產品慣例（台股，非西式）

- **K線蠟燭**：紅漲綠跌（`js/core/chart.js` 的 `candles`）。
- **損益金額文字顏色**（`pnlClass`，`js/utils/format.js`）：正值（獲利/上漲）紅色、負值（虧損/下跌）綠色、剛好0（平盤）維持預設白字——沿用同一套「紅漲綠跌」邏輯，不是西式的「綠漲紅跌」。
- **月份標籤**：民國年（`rocMonthLabel`，西元年 - 1911），例如 2026-06 → "115/06"。
- **K線週刻度**：不是固定「每5個交易日」或「每週一」，而是偵測「ISO週別變化後第一個實際出現的交易日」（`isoWeekKey`）——因為國定假日、颱風假補班等因素，一週的交易日數不固定。
- **K線標示（買賣點）**：只顯示價格數字，不加「買/賣」文字（顏色已經代表方向：紅=買、綠=賣）；時間相近的多筆標示會自動垂直錯開，避免重疊。
- **交易紀錄頁的部分賣出顯示**（`transactions.view.js` 的 `buildDisplayRows`）：一筆BUY如果只被部分SELL，不要顯示成兩筆看不出關聯的原始紀錄。改成依FIFO配對結果展開：已賣出部分顯示「已賣出X股＋實現損益」（日期用成交日），剩餘未賣部分顯示「剩餘X股＋殘值」（日期用買進日，殘值需已輸入現價）；整體仍依日期新到舊排序，兩者自然照時序排列。無法配對到任何買進的SELL（超賣）標示「⚠無對應買進」單獨顯示。`recompute()` 會多回傳 `openLots` 供這個畫面查每筆BUY的剩餘股數。
- **總覽頁個股分類**（`overview.view.js`）：不是單一「依標的分組績效」清單，而是3個可展開分類——獲利／虧損／持有（標籤不加「中」字）。判斷規則：**持有優先**（該標的目前有庫存，`positions` 裡查得到，不論過去已實現損益是正是負都算持有）；只有目前無庫存的標的才依已實現損益淨額分到獲利（>0）或虧損（<0）；淨額剛好0且無庫存的標的兩邊都不列（邊界情況，不特別處理）。點分類展開列出所屬個股，點個股導向詳細頁。
- **個股詳細頁**（`stock-detail.view.js`）：標題（名稱+代號）下方固定顯示一行「最後交易日 + 該筆成交價」（取該股最後一筆交易紀錄，不是市價）。
- **持股頁卡片版面**（`holdings.view.js`）：單行精簡版面，「更新」按鈕跟標的名稱/代號放同一行（右側），不要放在卡片最下面——使用者曾反映按鈕位置不好按、卡片太高。下面才接著持股股數/成本、市價/損益、市價輸入框。

## 資料來源（`js/data/marketdata.js`）

- **K線歷史資料**：FinMind（`api.finmindtrade.com`），免費版只有日K，且有 CORS 支援，可直接前端 fetch。
- **持股頁盤中即時報價**（`getLiveQuote`）三層備援，理由是任何單一公開/非官方端點都可能不穩：
  1. `TwseRealtimeProvider` — TWSE MIS（`mis.twse.com.tw`），依序試 `tse_`/`otc_` 前綴
  2. `YahooFinanceProvider` — 雅虎財經 chart API（`query1.finance.yahoo.com`，`.TW`/`.TWO`後綴）。**這個端點沒有 CORS 標頭**，因為這個 App 是純前端（無後端），改透過公開 CORS 代理轉發（`api.allorigins.win`、`api.codetabs.com` 依序嘗試）——這兩個代理本身也不穩定，實測時都出現過短暫 5xx，這是已知、能接受的風險，設計上任何一層失敗都回傳 `null` 而非拋錯，最終還有 FinMind 兜底。
  3. `FinMindProvider.getQuote` — 只在前兩者都失敗時才用，因為它本質是「最新一根日K收盤價」，只有收盤後才準確，盤中沒有意義。
- 雅虎股市（`tw.stock.yahoo.com`）網頁本身也沒有 CORS 標頭，一樣走不通，已測試過不用重測。
- **持股頁「全部更新」批次查詢的節流與逾時**：TWSE MIS 對過於密集的請求會節流/擋掉（使用者實測抓到過：批次更新逐檔各打一次會卡住），所以 `TwseRealtimeProvider.getQuotes()` 把同一市場前綴的所有代號合併成一次請求（`ex_ch=A|B|C`），且第二輪（otc補查）前會 `await sleep(TWSE_REQUEST_SPACING_MS)`（4.5秒間隔）才發下一個請求，不是緊接著打。另外這個模組所有 `fetch()` 都經過 `fetchWithTimeout()`（8秒逾時、用 `AbortController`）——因為公開端點或 CORS 代理曾經整個掛住不回應也不報錯，沒有逾時保護的話「全部更新」按鈕會卡在「更新中…」永遠轉不出來（使用者截圖抓到卡超過1分鐘），現在保證每次查詢在有限時間內一定會結束（失敗就換下一層備援）。

## 從截圖手動輸入交易紀錄（容易出錯，務必照下面流程）

實測證明：從手機截圖判斷「買賣別」這件事非常容易出錯（暗色主題+小字+壓縮畫質，紅綠字/漲跌停顏色常常看混，同一批資料曾經連續錯兩次：00631L/0052 買賣顛倒、00830 07/28 那筆也判斷錯），已經造成使用者多次來回確認、浪費額度。之後處理這類請求：

1. **優先要求文字型的官方對帳單，而不是互動查詢畫面的截圖**。對帳單通常會用明確的中文字「現股買進」「現股賣出」而不是只靠紅綠字，且會附精確的手續費/交易稅可以交叉核對金額（金額對不上就代表哪裡讀錯了）。
2. 股票代號一律用 FinMind 的 `TaiwanStockInfo` dataset（`api.finmindtrade.com`，已知可從此環境直接 fetch）核對名稱對應的代號，不要用記憶或猜測（`openapi.twse.com.tw`／`tpex.org.tw` 在這個沙盒會被擋，改用 FinMind）。
3. **寫入資料前，先用 `runFifo()` 跑一次完整驗證**：檢查每個標的最終剩餘股數，並主動列出來跟使用者核對「目前應該剩幾檔、各多少股」，而不是默默寫入就回報完成——這次就是靠使用者說「只剩3個標的」才抓出隱藏的錯誤。
4. 金額對得上不代表方向對：手續費/交易稅的加減方向（買進只扣手續費、賣出兩者都扣）本身無法用來反推買賣別，必須依賴文字或使用者明確確認。
5. 如果同一批資料裡已經修正過一次買賣別錯誤，對其餘所有列都要重新用最保守的方式再檢查一遍，不要預設「應該只有那一筆錯」。
6. 圖片一旦不在對話中重新附上，這個環境就無法重新叫出原圖比對——所以第一次讀圖時要更謹慎，把每一列的關鍵欄位（日期/代號/買賣別/價格/股數）明確列出來讓使用者當場核對，而不是直接拿去寫入資料庫。
7. **輔助工具：OCR＋像素顏色取樣，取代單純肉眼判斷顏色**。這個環境可以 `apt-get install -y tesseract-ocr tesseract-ocr-chi-tra` + `pip install pytesseract pillow` 跑中文OCR（已驗證可行）。買賣別的紅綠色不能只靠 OCR 文字本身（OCR 讀不出顏色），要用 `pytesseract.image_to_data()` 拿到每個文字方塊的座標，再用 `PIL.Image` 直接讀那個座標的像素RGB值，用程式判斷「紅色分量明顯較高」還是「綠色分量明顯較高」——這是客觀的數值判斷，用來取代容易看錯的肉眼判斷。之後遇到有顏色編碼買賣別的截圖，優先用這套流程輔助，而不是單純憑視覺判讀。

## 環境限制（不是程式問題）

- 這個沙盒的網路代理會擋掉對 `mis.twse.com.tw` 的連線（安全性限制），所以沙盒內無法直接測試 TWSE 這條路徑本身是否連得到；但這是台灣股市 App/網站廣泛使用的公開端點，失敗時的備援機制已經涵蓋這個情境。
- 週末/非交易時間不會有真正的盤中成交價，所有來源都一樣，這是正常現象不是 bug。
