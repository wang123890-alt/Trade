# 交易損益（Trade）App — 協作備忘

## 效率優先（重要，額度有限）

- **動手前先查現有資源，不重工**：使用者名下還有其他 repo（例如 `wang123890-alt/Choose`，一個台股篩選/分析工具），裡面可能已經踩過同樣的坑、驗證過同樣的資料來源或寫過類似邏輯。遇到資料來源、API串接、已知限制等問題時，**先查其他 repo 的程式碼或 `docs/` 底下的紀錄**，能直接引用結論就直接用，不要重新測試、重新踩雷、重新調研已經有答案的事。
- 使用者的方案額度有限、正在考慮是否續約，**浪費額度是實際成本**，不是抽象原則。每次操作前想一下「這步是必要的嗎，還是可以省略/合併」。
- 不要為了保險而重複驗證已經驗證過的東西（例如同一個 API 的 CORS 行為、同一段程式碼的既有邏輯）。
- **回報「XX功能不行」時，先讀該功能的前端邏輯，不要急著往網路層/API層找原因**：曾經連續3輪把「持股頁更新沒作用」診斷成TWSE節流、逾時、CORS問題（也確實都修了、也都是真的問題），但最後真正的根因其實是 `holdings.view.js` 單檔「更新」按鈕的**前端邏輯本身**——市價輸入框會預填上次的市價，按鈕邏輯是「輸入框有值就直接存那個值，只有輸入框是空的才會去打API」，導致**第一次成功後**，輸入框永遠不是空的，之後每次按「更新」都只是把同一個舊值存回去，完全沒有真的重新打API。這種「按鈕看起來沒反應、數字沒變」的症狀，要先懷疑「根本沒有觸發預期的程式路徑」，而不是預設「API呼叫失敗了」——讀一次相關的 click handler 完整邏輯，比連續猜三輪網路層原因更快也更準。
- **「彼此等價的嘗試」用平行，「有優先高低的來源」用序列——這兩件事不要混為一談**（這條分界線是踩過兩次坑才畫出來的，兩邊都有血）：
  - **平行的是同一層裡彼此等價、誰先回來都一樣好的嘗試**：多個CORS中繼競速、`.TW`／`.TWO` 兩種市場猜測、「全部更新」時多檔股票同時查。曾經把「直接打→代理A→代理B」寫成依序 `for` 迴圈＋逐一 `await`，每層各自的逾時疊加起來最壞可以到80秒以上，使用者會覺得「卡住」——改平行後壓到10幾秒內。
  - **序列的是彼此不等價、有明確優先順序的來源**：證交所 → 雅虎 → FinMind。這幾層**不能**平行，因為雅虎的台股報價本來就是證交所轉手來的，同時問等於花成本要一份源頭已經在給的、而且比較差的複製品（實測2454差30點）；而源頭真的沒資料時下游也不會有，平行救不到。詳見「資料來源」章節。
  - 判準：**問自己「這幾個嘗試拿回來的東西一樣好嗎」**。一樣好（只是快慢/通不通不同）→ 平行競速；有高低之分 → 序列，而且要讓每一層都有自己的逾時上限，避免某一層掛住拖垮整串。
- **「回來了但沒生效」要懷疑「呼叫成功了，但結果被靜默丟棄」，不是只懷疑「呼叫失敗」**：`getLiveQuotes()`（批次版）設計成「絕不拋錯」，全部來源都查無資料時就回傳 `{}`；但 `holdings.view.js`「全部更新」的呼叫端邏輯是「有拿到價格才寫入，`catch` 只接得住真正拋出的例外」——`{}` 不是例外，所以沒有寫入、也沒有跳出任何錯誤訊息，使用者只會看到按鈕恢復正常、但價格完全沒變，跟「卡住」一樣難排查，但成因完全不同（不是還在等，是已經做完了什麼都沒做）。**每次幫一個「絕不拋錯、用空結果表示失敗」的函式接上呼叫端時，一定要顯式檢查空結果並給使用者看得到的回饋（alert/畫面文字），不能只包一層 `try/catch` 就以為涵蓋了失敗情境。**

## 從無名稱/代號的K線截圖反查是哪支股票

- **全市場反查（用收盤價找股票）在這個環境做不到，不用每次重測**：`openapi.twse.com.tw` 被沙盒代理擋掉、TPEx OpenAPI 連不到、FinMind 免費版查全市場某天所有股票會被拒絕（免費版必須指定單一 `stock_id`），三條路都試過且都不通。
- **但 `wang123890-alt/Choose`（台股篩選工具）常常已經幫忙縮小範圍**：它的 `docs/validation/*.md` 驗證紀錄裡有多份「篩選結果清單」（例如「30日反彈型」「成交量集中族群」等），列出的是近期實際跑過篩選、真的有異動的個股代號。截圖裡的波段特徵（起漲低點、前波高點、漲幅型態）常常能跟這些清單對上。**收到沒有名稱/代號的K線截圖時，先看 Choose repo 這些清單裡有沒有型態相符的候選，再用 FinMind 逐一核對候選的 `TaiwanStockPrice`（單一 `stock_id` 免費版可查），比盲猜或跟使用者要求更多截圖更快。**
- 曾經漏掉的案例：截圖顯示波段起漲98.30、前波高點248、當日234跌0.64%，其實 Choose repo 驗證紀錄的「30日反彈型」清單裡已經列了「2351順德」，型態完全對得上，但仍花了一輪問使用者才確認——下次同類截圖要先比對清單。

## PR 流程

- 分支：`claude/trade-records-pnl-6cbsak`，對 repo `wang123890-alt/Trade`（public）。
- 每個功能改動開一個 PR（draft），完成後：
  - **無特殊需求或必要（無衝突、測試全過、無需要跟使用者確認的設計決策），直接合併，不用每次都問。**
  - 只有在真的有衝突、測試失敗、或設計上需要使用者決定時，才停下來問。
- 合併後固定流程：
  1. `git fetch origin main`
  2. 比對舊 commit 與新 `origin/main` 的 diff——如果只有 `data/store.json`（使用者手機/電腦透過 App 自動同步的交易資料）變動或完全無差異，代表可以安全對齊
  3. `git reset --soft origin/main`（絕不用 `--hard`，避免弄丟本地未推送的東西）
  4. `git push --force-with-lease origin claude/trade-records-pnl-6cbsak`
  5. 用 `curl` 輪詢 GitHub Pages 確認新內容已上線，再跟使用者說可以測試（瀏覽器快取可能落後，必要時提醒強制重新整理）
- main 分支偶爾會被 App 本身的 GitHub 同步功能直接寫入 `data/store.json`（"更新交易資料" commit，作者是使用者本人，透過手機/電腦的 App 觸發）——這是正常的資料同步，不是別人動了程式碼，diff 只會動到那個檔案。

## 測試

- 每次改動後跑 `node tests/*.test.js`（純 Node，無框架，逐檔執行），全部要過才能推送。
- `renderKLineChart`（`js/core/chart.js`）沒有專門的測試檔，改動後用臨時腳本（`node --input-type=module -e "..."`）驗證輸出的 SVG 字串。
- **K線SVG絕對不要同時給固定像素 `width`/`height` 屬性和 `viewBox`**：曾經寫成 `width="100%" height="${totalHeight}"`（`totalHeight` 是 viewBox 座標系的數字），這會讓瀏覽器渲染框的寬高比（容器寬 × 固定高）跟 viewBox 本身的寬高比不一致，`preserveAspectRatio` 預設會等比縮放置中，導致窄螢幕上下留下大片空白（使用者截圖抓到過這個問題）。正確做法：只設 `viewBox`，`style` 給 `width:100%; height:auto;`，讓高度依 viewBox 比例自動算。

## 安全

- **絕對不要**接受、記錄、或使用使用者在對話中貼的 GitHub PAT 或其他密鑰。若使用者不慎貼出，立刻提醒撤銷/重新產生，並說明新的 token 只能直接輸入到 App 本身的 Settings 頁面（裝置端），不能再透過對話傳遞。
- **`innerHTML` 未跳脫的 `<`/`>` 不只是資安問題，本身就是會讓瀏覽器誤解DOM結構的functional bug**——不要以為「這段文字是App自己算出來的、不是使用者輸入」就可以跳過 `escapeHtml()`。2026-09-17 實測踩過一次：`stock-detail.view.js` 的「參考價位」區塊把 `computeTradeLevels()` 算出的 `trendLabel`（硬編碼字串，但字面含 `"MA5<MA20<MA60"`）直接串進 `innerHTML` 沒跳脫，使用者回報「畫面全部擠成一行橫向溢出」，強制重新整理也沒用（不是快取問題）。用 Playwright 載入真實頁面逐層比對DOM才找到根因：瀏覽器把 `<MA20` 當成未知標籤開頭，導致後面所有本來該是同層兄弟節點的區塊全部被吞成那個偽標籤底下的子節點——用最小案例（`el.innerHTML = 'A <span>x</span> 文字<B<C）B row<div>C row</div>'`）就能重現「兄弟節點數從3變1」。**教訓：任何要塞進 `innerHTML` 的動態字串（不論來源是使用者輸入還是App自己算出來的），一律過 `escapeHtml()`，不要憑「這是我自己的字串應該安全」的直覺跳過。** 判斷「畫面排版跑版、強制重新整理無效」這類回報時，先懷疑是不是有裸 `<`/`>` 混進了某段動態文字，比一路懷疑CSS更快找到根因。

## 產品慣例（台股，非西式）

- **K線蠟燭**：紅漲綠跌（`js/core/chart.js` 的 `candles`）。
- **損益金額文字顏色**（`pnlClass`，`js/utils/format.js`）：正值（獲利/上漲）紅色、負值（虧損/下跌）綠色、剛好0（平盤）維持預設白字——沿用同一套「紅漲綠跌」邏輯，不是西式的「綠漲紅跌」。
- **月份標籤**：民國年（`rocMonthLabel`，西元年 - 1911），例如 2026-06 → "115/06"。
- **K線週刻度**：不是固定「每5個交易日」或「每週一」，而是偵測「ISO週別變化後第一個實際出現的交易日」（`isoWeekKey`）——因為國定假日、颱風假補班等因素，一週的交易日數不固定。
- **K線標示（買賣點）**：只顯示價格數字，不加「買/賣」文字（顏色已經代表方向：紅=買、綠=賣）；時間相近的多筆標示會自動垂直錯開，避免重疊。
- **K線技術指標子面板（RSI／MACD／DMI）**：`js/core/indicators.js` 的 `computeMACD`（快線12/慢線26/訊號9，標準EMA，`emaSeries()` 是共用的通用EMA實作，同時吃「收盤價」這種無空值的序列跟「MACD線」這種前面會有null的序列）與 `computeDMI`（Wilder平滑，period 14，回傳 `plusDI`/`minusDI`/`adx`）；`renderKLineChart`（`js/core/chart.js`）新增 `rsi`/`macd`/`dmi` 三個選填參數，有傳才會在價格圖與成交量下方各自疊一個子面板（`renderSubPanel`，各自獨立的Y軸刻度、參考虛線、面板內文字標籤），共用同一條X軸座標映射（`xAt`），所以三個子面板跟蠟燭圖的時間刻度永遠對得整齊。子面板順序固定是MACD → DMI → RSI（RSI放最下面，使用者要求的順序）。MACD柱狀圖沿用「紅漲綠跌」邏輯：柱狀值（DIF-DEA）為正算多方動能算紅、為負算綠；DMI的+DI/-DI也套用同一套邏輯（+DI紅、-DI綠、ADX用中性的`text-faint`色）。三個指標都沒傳時完全不畫子面板、SVG高度不變（向下相容，`renderKLineChart` 舊呼叫端不用改）。個股詳細頁「訊號說明」區塊同時多了MACD黃金/死亡交叉、DMI的+DI/-DI交叉、ADX≥25時的趨勢力道提示，沿用既有MA交叉/RSI超買超賣的同一套 `detectMACross()`（這函式本來就是純粹比較兩條數列在某個index的相對位置，不限定要MA，MACD線vs訊號線、+DI vs -DI都能直接套用，不用另外寫）。
- **交易紀錄頁的部分賣出顯示**（`transactions.view.js` 的 `buildDisplayRows`）：一筆BUY如果只被部分SELL，不要顯示成兩筆看不出關聯的原始紀錄。改成依FIFO配對結果展開：已賣出部分顯示「已賣出X股＋實現損益」（日期用成交日），剩餘未賣部分顯示「剩餘X股＋殘值」（日期用買進日，殘值需已輸入現價）；整體仍依日期新到舊排序，兩者自然照時序排列。無法配對到任何買進的SELL（超賣）標示「⚠無對應買進」單獨顯示。`recompute()` 會多回傳 `openLots` 供這個畫面查每筆BUY的剩餘股數。**每列的標的名稱是可點的連結**，點了會導向個股詳細頁（K線／看法紀錄／虧損覆盤）——原本這頁完全沒有連結出去，是使用者發現「交易頁的個股沒有K線」才補上，跟持股頁、觀察頁的 `data-action="view-detail"` → `navigate('detail', stockId)` 走同一套模式。
- **總覽頁個股分類**（`overview.view.js`）：不是單一「依標的分組績效」清單，而是3個可展開分類——獲利／虧損／持有（標籤不加「中」字）。判斷規則：**持有優先**（該標的目前有庫存，`positions` 裡查得到，不論過去已實現損益是正是負都算持有）；只有目前無庫存的標的才依已實現損益淨額分到獲利（>0）或虧損（<0）；淨額剛好0且無庫存的標的兩邊都不列（邊界情況，不特別處理）。點分類展開列出所屬個股，點個股導向詳細頁。**獲利／虧損分類下每檔的次要說明文字是「N 筆 · 損益率 X%」，不是勝率**——`computeRealizedSummary`（`js/core/statistics.js`）新增 `realizedPnLPercent` 欄位，是「該標的所有已實現配對的總損益 ÷ 總買進成本」（用配對本身已有的 `buyCost` 加總），是成本加權的整體報酬率，不是每筆損益率的簡單平均（一筆大部位賺10%、一筆小部位虧90%，加權後應該接近+10%而不是平均出來的-40%）。`winRate`（勝率）欄位還留著，其他地方（例如 `groupByStrategy` 的策略統計）仍在用，只是總覽頁個股分類這裡改顯示損益率，因為使用者覺得「這檔賺多少/賠多少」比「勝率」更直接。
- **個股詳細頁**（`stock-detail.view.js`）：標題（名稱+代號）下方固定顯示一行「最後交易日 + 該筆成交價」（取該股最後一筆交易紀錄，不是市價）。
- **持股頁卡片版面**（`holdings.view.js`）：單行精簡版面，「更新」按鈕跟標的名稱/代號放同一行（右側），不要放在卡片最下面——使用者曾反映按鈕位置不好按、卡片太高。下面才接著持股股數/成本、市價/損益、市價輸入框。
- **價格與K線都附註「來源 · 更新時間」**：持股頁市價旁邊用「雅虎 · 14:32更新」（只有時間，`formatTime`）；個股詳細頁K線標籤列旁邊用「雅虎 · 09/14 14:32更新」（加了日期，`formatDateTime`，兩者都在 `js/utils/format.js`）——K線是使用者切換到別的標的、過一陣子才會再回來看的畫面，只顯示時間分不出是今天還是昨天抓的，價格則是同一次操作內立即看到結果，時間就足夠。這是在「來源標籤」（見上面資料來源章節）之上再加一層——標籤只回答「這數字是不是收盤價」，時間戳記回答「這是不是剛剛才抓的」。都是**當次 session 記憶體內**（`holdings.view.js` 的 `priceMeta`、`stock-detail.view.js` 的 `renderChartFromBars` 參數），重新整理頁面就會消失，只有價格本身會持久化。
- **持股頁「AI解析」欄位與Excel匯出**：每檔持股卡片下方有一個可展開的「AI解析」文字欄位（`StockAiAnalysisRepository`，`js/data/storage.js`），單一可覆寫的自由文字欄，跟「看法紀錄」（`StockNotesRepository`，append-only 時間軸、使用者自己寫的）是分開的兩個概念，不要混用。「匯入AI解析」讓使用者一次貼上涵蓋多檔股票的AI分析文字，`classifyAiAnalysisText`（`js/core/aiAnalysisImport.js`）有兩種模式，依貼上的文字長相自動選：**有「---」分隔線時走「區塊模式」**——先用「---」切成一個個區塊，每個區塊整塊歸給「區塊自己的標題」認定的那支股票，區塊內文字不會因為提到別支股票就被搶走；沒有分隔線時才退回舊的「逐行模式」（沿用上一次提到的股票）。**區塊模式是實測真實AI回覆後才加的**：一份用「---」分段的真實9檔持股分析報告裡，2330台積電幾乎在每一支其他股票的段落裡都會被提到一次（例如2454聯發科自己的段落寫「目前明顯不如台積電強」），逐行模式會把「current stock」誤切換成2330，導致大半份報告被黏到2330名下、其他股票的段落反而被搬空。區塊模式改成「認區塊自己的標題」（`isHeaderLineFor`：那一行扣掉代號和名稱後幾乎不剩字才算標題，例如「2330 台積電」算，但「目前明顯不如台積電強」不算），標題不明確時退而求其次數這個區塊裡各股票被提到的次數，只有明顯獨占（次數領先第二名兩倍以上，或整個區塊只提到一檔）才歸類，像「持股最終排名」表格那種平均提到每一檔的區塊會落到「未比對到」而不是隨便黏給第一檔——這樣才不會把「排名表」「今天優先順序清單」這種本來就跨股票的共用內容誤判成某一檔的分析。完全比對不到任何持股的文字歸在「未比對到」區塊只顯示不寫入。分類結果一定先預覽（可個別勾選、可編輯每檔文字）才寫入，不會盲目自動覆蓋——寫入時如果該股已有舊內容，新內容用日期分隔線附加在後面而不是覆蓋掉。**單股卡片下方直接貼上詳解，行為跟批次匯入一致**（`appendAiAnalysis`，`js/core/aiAnalysisImport.js`，兩條路共用同一個函式）：展開後舊內容用唯讀方式顯示在上面，下面是一個空白的「貼上這檔的AI詳解…」文字框，按儲存一律是「附加」不是覆蓋整欄——這是因為原本的設計是單一文字框直接預填舊內容、使用者打字後儲存＝整欄覆蓋，跟批次匯入的「累加＋日期分隔線」不一致，使用者問了才發現這個落差，改成兩條路都用同一套累加邏輯。「匯出Excel」（`js/utils/exportExcel.js` 的 `buildExcelXml`/`downloadExcel`）用 SpreadsheetML（Excel 2003 XML格式，副檔名 `.xls`）純字串組出來，不需要額外套件或zip編碼，Excel可以直接開；欄位含代號/名稱/股數/成本均價/市價/未實現損益/損益%/AI解析。**匯出的檔案是設計給使用者拿去餵外部AI分析用的**，所以在資料表格上方多放一列說明文字（`buildExcelXml` 的 `note` 參數，用 `ss:MergeAcross` 橫跨所有欄位），內容是明確要求AI「回覆時每檔股票另起一段、且每段開頭要標代號」——這樣使用者把AI的回覆貼回「匯入AI解析」時，`classifyAiAnalysisText` 才能逐行分類乾淨，不會把沒標代號的共用建議段落誤黏到最後一次提到的股票（這是後來實測「持股總覽＋基本面分析」這種真實格式的AI回覆時發現的邊界情況：沒有標代號的開頭免責聲明/結尾共用建議，會被歸到最後一次提到的個股名下）。**區塊模式後來又補了一種介於「有分隔線」跟「逐行模式」之間的真實情況（2026-09-17 使用者實測回報「AI分析沒有比對到代碼」）**：有些AI報告只在開頭放**一個**「---」把intro跟後面整份清單分開，清單裡9檔各自一段（用空行分隔，不是每檔都夾一條「---」），且每段開頭直接是「代號 名稱：分析文字」全部擠在同一行——這剛好符合Excel匯出說明要求AI的格式，但不符合`isHeaderLineFor`要求的「乾淨獨立標題行」。結果整個intro後的清單被當成同一個「---」區塊，區塊內段落又互相提到對方（例如0050段落提到「你已有2330與正2」），提及次數變得接近、判不出誰獨占，整塊落到「未比對到」。修法：`ownerOfBlock()`（整個「---」區塊層級）維持原本嚴格判斷不變；只有整塊判不出來時才退而用**空行**拆成段落，段落層級改用`ownerOfParagraph()`，多一層`startsWithStock()`（段落開頭是代號/名稱就算，不要求整行只剩代號名稱）。**這個寬鬆檢查只能用在「已經確定是單一段落」的層級，不能提早用在還沒拆段落的整個區塊層級**——中間有一版直接在區塊層級套用寬鬆檢查，導致區塊第一行剛好是第一段的標題，把後面8段全部吞給第一檔，是用使用者的實際資料重新測試才抓到的（`tests/aiAnalysisImport.test.js` 已補上這個格式的回歸測試）。

## 資料來源（`js/data/marketdata.js`）

- **K線歷史資料**：`YahooFinanceProvider.getKLine` 優先、`FinMindProvider.getKLine` 備援（`stock-detail.view.js`）。**FinMind 免費版日K會落後好幾天**（2026-09-14 實測：FinMind 最新一筆停在09-11，雅虎經 r.jina.ai 同時間點已經有09-14當天的即時中盤資料），這是「K線沒更新、只有價格有更新」這個回報的根因——價格走 `getLiveQuote()`（雅虎優先）所以是新的，K線當時還是單獨走 FinMind 所以停在好幾天前。雅虎 chart API 的 `range` 參數只能選固定區間（`5d`/`1mo`/`3mo`/`6mo`/`1y`/`2y`/`5y`），`yahooRangeFor()` 依需要的起始日期挑最接近的；回傳資料某些區間會用 `close:null` 補非交易日，`parseYahooChartBars()` 會把這種列過濾掉，不會畫出假的0元蠟燭。FinMind 仍保留當備援，因為它不需要中繼、CORS 直接能打。
- **持股頁盤中即時報價**（`getLiveQuote`/`getLiveQuotes`）順序是 **證交所 → 雅虎 → FinMind，嚴格照順序試，證交所還在回答時不會去問任何其他來源**：
  1. `TwseRealtimeProvider` — 證交所 MIS（`mis.twse.com.tw`）。**雅虎的台股報價本來就是從證交所來的，直接打源頭少一手轉述**；實測差異（2026-09-15 12:05 同一瞬間）：2330 兩邊都是2390、0050 差0.15、**2454 證交所4505 vs 雅虎4535，差了30點**——雅虎會落後，波動大的時候差很多。多檔一次只發**一個**請求（`ex_ch` 用 `|` 串起所有代號的 tse_/otc_ 兩種猜測）。
  2. `YahooFinanceProvider` — 證交所被擋或還沒回來時用。`.TW`／`.TWO` 兩種猜測平行發送取最快的。
  3. `FinMindProvider.getQuote` — 只是安全網，本質是「最新一根日K收盤價」，盤中不會變。
- **證交所優先就是真的優先：不要跟雅虎平行發送，也不要「為了保險」同時問兩邊**（這是使用者明確要求的設計，2026-09-15）。理由是源頭與轉手的關係：雅虎的台股報價本來就是從證交所轉來的，同時問兩邊等於花成本去要一份源頭已經在給我們的資料的複製品，而且那份複製品還比較差（實測2454：證交所4505 vs 雅虎4535）。**「多一層平行比較保險」在這裡是錯的直覺**——如果證交所真的出事沒資料，下游的雅虎同樣不會有資料，平行並不會多救到什麼。
- **證交所這條路是穩的，不要把 2026-09-14 那次失敗當成「證交所會翻臉」**（這個歸因錯誤本身就是個教訓）：那天記錄的三個數字沒有一個是證交所拒絕我們——`r.jina.ai` 回 **401「bad IP reputation」是中繼嫌呼叫端的IP、是它拒絕我們**，訊息根本沒到證交所；`codetabs` 回 522 是它自己掛了（那天它連打雅虎都 0/5，後來整個移除）；`allorigins` 回 408，而它打雅虎也只有 2/5，本來就不穩。真正來自證交所的拒絕只有一種、而且穩定可解釋：**直連（機房IP）會收到證交所自己的 WAF 擋人頁（502）**，但**經 `r.jina.ai` 走就通**，因為那些IP證交所接受（2026-09-15 實測 5/5、每次3.0–4.8秒）。**所以脆弱的是中繼那一層，不是證交所。**
- **雅虎那層留著，救的是「中繼那層失靈害我們連不到」，不是「證交所沒資料」**：證交所真的沒資料時下游都沒有，那種情況救不到；但中繼有壞掉的前科（就是09-14），那時證交所本身好好的、雅虎也照常有資料，序列往下走才有意義。
- **看到「證交所拿不到」時，先確認是不是自己方法錯，不要直接推論成「這條路不能用」**：09-14 的結論就是這樣被推論錯的，連帶讓這個功能停用了整整一天。已知的方法錯誤有三個，都記在下面：中繼選錯（用掛掉的 codetabs／不穩的 allorigins 而不是 r.jina.ai）、`z` 是 `'-'` 時沒去看 `trade.z`、以及誤以為沙盒擋掉 `mis.twse.com.tw` 的連線（見「環境限制」那段的更正）。
- `TWSE_ATTEMPT_TIMEOUT_MS`（6秒）是「試多久就放棄往下走」，不是在懷疑證交所——這個模組每個 `fetch` 本來就都有逾時保護，只是避免某次請求掛住讓整個更新卡死。這條路只有使用者按「更新」才會跑、不是定時輪詢（跟 `wang123890-alt/Choose` 的 D31 決策同一個分寸：使用者觸發、小批次、有冷卻、絕不排程輪詢——那份文件也記了證交所MIS是它自家前端API、`robots.txt` 全站 disallow，所以個人自用可以，高頻輪詢不行）。
- **「全部更新」的批次是證交所一個請求打完整個投組**，只有證交所沒給出盤中價的那幾檔才往下走雅虎/FinMind；那些漏網的彼此之間才是平行（十檔漏網跟一檔差不多時間）——**那是「很多檔同時跑」，不是「同一檔同時問兩個來源」**，兩件事不要混。實測9檔全部由證交所一個請求解決、195毫秒完成，完全沒有發出任何雅虎請求。
- **證交所的空值是字串 `'-'`，不是 null 也不是 0，而且 `z`（最新成交價）盤中常常是空的**：2026-09-15 12:03 實測 2454 的 `z` 是 `'-'`，但它明明正在交易（`o` 4505／`h` 4575／`l` 4465／`v` 4007）——真正的最後成交價在巢狀的 `trade.z`（4495，成交時間12:01:56）。所以 `parseTwseRow()` 的順序是 `z` → `trade.z` → 最後才是 `y`（昨收）。**這個順序錯了會很嚴重**：第一版只看 `z`、失敗就退回 `y`，結果9檔裡有4檔在盤中顯示昨收（2454會顯示4560，而它實際上正在4495交易）。同理，**證交所的「昨收」不可以贏過雅虎的盤中價**：`getLiveQuote()` 只有在 `isIntraday` 為 true 時才採用證交所的答案，昨收要排在雅虎後面（雅虎也沒有時才用）。
- **中繼清單 `CORS_PROXIES` 的順序是實測出來的，不要憑感覺改**（2026-09-14 同一分鐘、同一目標）：
  | 中繼 | 成功率 | 耗時 |
  |---|---|---|
  | `r.jina.ai` | **5/5** | **0.53–0.76秒**，且會回應 `Access-Control-Allow-Origin` 給呼叫端網域 |
  | `api.allorigins.win` | 2/5 | 成功時 3.6秒 與 **19.3秒** |
  | `api.codetabs.com` | 0/5 | 全部 522，服務本身掛了（已移除）|
  （以上是打**雅虎**的數字。同一個 `r.jina.ai` 打**證交所**也是通的：09-15 實測 5/5、每次3.0–4.8秒。09-14 那天三個中繼一起出狀況，是中繼層的問題不是證交所的——詳見上面「證交所這條路是穩的」那條。）
  `r.jina.ai` 回傳的是 `text/plain`，JSON 前面有一段 `Title: / URL Source: / Markdown Content:` 標頭，所以整個模組改用 `parseJsonLoose(await response.text())`（找第一個 `{` 到最後一個 `}`）而不是 `response.json()`。
- **`PROXY_TIMEOUT_MS` 是 12 秒不是 6 秒**：中繼是平行競速，逾時只限制「落後者還能再試多久」，快的那個早就回來了，所以拉長幾乎零成本；但設成 6 秒會把 allorigins 那種 19 秒才回來的成功回應直接砍掉，整條鏈路就掉到 FinMind 的舊收盤價。
- **每筆報價都帶 `source` 標籤**（`證交所` / `證交所昨收` / `雅虎` / `09/12收盤`），持股頁會顯示在價格旁邊。這是為了回答那個反覆出現的問題：「我按了更新，數字沒動，是不是壞了？」——標著「收盤」的數字本來就不會動（那是已結算的收盤價），跟更新失敗是兩回事，一眼就能分辨。
- **失敗要讓使用者看得到原因，不要吞掉**：`TwseRealtimeProvider`/`YahooFinanceProvider` 設計上「絕不拋錯」，失敗就回傳 `null`；但最後一層 `FinMindProvider.getQuote` 如果拋錯，`getLiveQuote()` 現在會讓它繼續往外傳（不吞掉），單檔「更新」按鈕的 `alert` 會直接顯示 `err.message`，而不是統一顯示「自動取得市價失敗」這種看不出原因的訊息。三層都真的查無資料時，`getLiveQuote()` 自己會丟出一個說明「證交所、雅虎財經、FinMind 都查無這檔的價格資料」的錯誤，而不是回傳 `null` 讓呼叫端猜；但**如果證交所至少給得出昨收，就回傳那個昨收而不是拋錯**（有數字總比沒數字好，FinMind 的具體錯誤訊息只在完全沒數字時才往外丟）。「全部更新」（`getLiveQuotes`）同理：全部落空時會回傳 `{}`（設計上不拋錯），呼叫端要顯式檢查空結果並跳出提示，不能只靠 `try/catch`（`{}` 不是例外，`catch` 接不到）。
- 雅虎股市（`tw.stock.yahoo.com`）網頁本身也沒有 CORS 標頭，一樣走不通，已測試過不用重測。
- **逾時設計**：這個模組所有 `fetch()` 都經過 `fetchWithTimeout()`（`AbortController`）。實際數值：`DIRECT_TIMEOUT_MS=3000`（直接嘗試）、`PROXY_TIMEOUT_MS=12000`（中繼較慢，且它們是平行競速所以拉長幾乎零成本，見上面那條）、`KLINE_TIMEOUT_MS=10000`（K線非互動式、有自己的「載入中」畫面）、`TWSE_ATTEMPT_TIMEOUT_MS=6000`（證交所這一層試多久就放棄往下走）。**注意這裡的平行/序列分工**：同一層內彼此等價的嘗試（多個中繼、`.TW`/`.TWO`、多檔股票）平行競速；不同來源之間（證交所→雅虎→FinMind）序列，靠各自的逾時上限保證不會拖垮整串。全部都失敗的最壞情況約在10幾秒內一定會結束，不是無限卡住。
- **持股頁「更新」按鈕不再有市價輸入框（手動輸入市價）**：使用者反映抓取失敗時猜還要保留手動輸入的複雜度沒必要，直接拿掉——「更新」失敗就跳提示，不提供手動備援輸入。

## 從截圖手動輸入交易紀錄（容易出錯，務必照下面流程）

實測證明：從手機截圖判斷「買賣別」這件事非常容易出錯（暗色主題+小字+壓縮畫質，紅綠字/漲跌停顏色常常看混，同一批資料曾經連續錯兩次：00631L/0052 買賣顛倒、00830 07/28 那筆也判斷錯），已經造成使用者多次來回確認、浪費額度。之後處理這類請求：

1. **優先要求文字型的官方對帳單，而不是互動查詢畫面的截圖**。對帳單通常會用明確的中文字「現股買進」「現股賣出」而不是只靠紅綠字，且會附精確的手續費/交易稅可以交叉核對金額（金額對不上就代表哪裡讀錯了）。
2. 股票代號一律用 FinMind 的 `TaiwanStockInfo` dataset（`api.finmindtrade.com`，已知可從此環境直接 fetch）核對名稱對應的代號，不要用記憶或猜測（`openapi.twse.com.tw`／`tpex.org.tw` 在這個沙盒會被擋，改用 FinMind）。
3. **寫入資料前，先用 `runFifo()` 跑一次完整驗證**：檢查每個標的最終剩餘股數，並主動列出來跟使用者核對「目前應該剩幾檔、各多少股」，而不是默默寫入就回報完成——這次就是靠使用者說「只剩3個標的」才抓出隱藏的錯誤。
4. 金額對得上不代表方向對：手續費/交易稅的加減方向（買進只扣手續費、賣出兩者都扣）本身無法用來反推買賣別，必須依賴文字或使用者明確確認。
5. 如果同一批資料裡已經修正過一次買賣別錯誤，對其餘所有列都要重新用最保守的方式再檢查一遍，不要預設「應該只有那一筆錯」。
6. 圖片一旦不在對話中重新附上，這個環境就無法重新叫出原圖比對——所以第一次讀圖時要更謹慎，把每一列的關鍵欄位（日期/代號/買賣別/價格/股數）明確列出來讓使用者當場核對，而不是直接拿去寫入資料庫。
7. **輔助工具：OCR＋像素顏色取樣，取代單純肉眼判斷顏色**。這個環境可以 `apt-get install -y tesseract-ocr tesseract-ocr-chi-tra` + `pip install pytesseract pillow` 跑中文OCR（已驗證可行）。買賣別的紅綠色不能只靠 OCR 文字本身（OCR 讀不出顏色），要用 `pytesseract.image_to_data()` 拿到每個文字方塊的座標，再用 `PIL.Image` 直接讀那個座標的像素RGB值，用程式判斷「紅色分量明顯較高」還是「綠色分量明顯較高」——這是客觀的數值判斷，用來取代容易看錯的肉眼判斷。之後遇到有顏色編碼買賣別的截圖，優先用這套流程輔助，而不是單純憑視覺判讀。

## 環境限制（不是程式問題）

- ~~這個沙盒的網路代理會擋掉對 `mis.twse.com.tw` 的連線~~ **這句是錯的，2026-09-15 實測推翻**：`curl` 直接打 `mis.twse.com.tw` 是通得到的，回來的是**證交所自己的 WAF 擋人頁**（HTTP 502 +「因為安全性考量，您所執行的頁面無法呈現」+ 錯誤代碼），不是沙盒攔截。也就是說擋人的是證交所（擋機房IP），不是這個環境。而且**經 r.jina.ai 中繼反而打得通**（當天 5/5 成功），所以沙盒內是**可以**測證交所這條路的，用 `node --input-type=module -e "import {TwseRealtimeProvider} ..."` 或 `curl https://r.jina.ai/https://mis.twse.com.tw/...` 就能測。
- **沙盒裡的 Playwright 瀏覽器完全沒有對外網路**（連 `fonts.googleapis.com` 都 `net::ERR_CONNECTION_RESET`）——只有 `curl`／`node` 走得通代理。所以**瀏覽器端的真實網路測試在這裡做不到**，Playwright 只能拿來測有 mock 的 UI 邏輯。曾經差點把這個沙盒限制誤判成「修正無效」，記住這個區別：
  - 要驗證報價鏈路本身能不能拿到資料 → 用 `node --input-type=module -e "import {getLiveQuote} ..."`（會走代理，測得到真實網路）
  - 要驗證 CORS 標頭 → 用 `curl -D - -H "Origin: https://wang123890-alt.github.io"` 看回應有沒有 `access-control-allow-origin`
  - 要驗證畫面邏輯 → Playwright ＋ 蓋掉 `window.fetch`
- 週末/非交易時間不會有真正的盤中成交價，所有來源都一樣，這是正常現象不是 bug。
- **收到「某頁的功能沒更新/沒生效」，且該頁其實跟已經修過的頁面共用同一段程式碼時，先確認GitHub Pages上線的版本，不要預設是程式又壞了**：例如觀察頁的K線點進去用的就是持股頁/個股詳細頁那套已經修過的 `stock-detail.view.js`，沒有獨立實作。用 `curl https://wang123890-alt.github.io/Trade/js/features/<檔名> | grep <關鍵字>` 直接看部署上的內容有沒有包含修正，比再猜一輪「是不是又有新的根因」快，通常答案是「使用者測試時瀏覽器快取還是舊版」，提醒強制重新整理即可。

## 多AI協作公約（Claude × Grok）

使用者同時讓 Claude 跟 Grok 在這個repo上做事，兩邊有機會改到同一個檔案（2026-09-17 就真的撞過：`js/core/tradeLevels.js` 跟它的測試檔同時是雙方的工作範圍）。以下規則兩邊都要守，不是只對其中一方的要求。**Grok看過這份公約後的回應、補充（分工表、清註解前的補救辦法、「先記錄」vs「寫入」的口令定義）記在 `docs/collaboration-covenant.md`，兩邊接手前都該一併看過。**

1. **動手前先看對方最近做了什麼**：改動前 `git fetch --all` 看一下 `git log --all --oneline -10` 跟 `git branch -r`，確認有沒有 `grok/*`、`claude/*` 之類的分支或main上的新commit動到自己要改的檔案，避免兩邊同時改、互相蓋掉，或是重工做已經被驗證過的事。
2. **有疑慮、或牽涉到「使用者還沒拍板」的設計決策，先寫成draft/docs記錄結論，不要直接push進main**——如果文件裡自己寫了「先回測、看結論再寫入」「未寫入main」這種但書，就要真的等使用者明確說可以了才動手，不能自己記錄完又自己執行。2026-09-17 發生過這個落差（草稿寫著不要逕自push，結果程式碼還是被push了），後來確認是使用者看過草稿後口頭批准的，不是自動化流程的疏漏，但這個但書本身要當真，不要是形式。
3. **重大邏輯改動要留得住「為什麼」，不是只留「做了什麼」**：不管是寫在commit訊息、docs檔案還是程式碼註解裡，至少要有一個地方講清楚一個數字或規則是怎麼測出來的、為什麼選這個不選那個。改動時如果會清掉對方寫的說明或註解，要先確認那些內容有沒有被搬到別的地方保留下來，不要淨刪除——2026-09-17 Grok那次改動把 `tradeLevels.js` 裡大量逐行的backtest依據註解清空、只留一行連結到docs檔案，邏輯本身沒錯，但之後任何一方要接手改這段程式碼，都得先想到去翻文件而不是只看程式碼旁邊的註解，容易漏掉。
4. **測試門檻共用**：push前 `node tests/*.test.js` 全部要過，不管是誰改的、改多小。
5. **同一個功能如果兩邊有不同結論，不要互相覆蓋掉對方的決定**：例如「參考停損」原本是Claude驗證出的1.0×ATR結構停損，Grok那輪改成主看MA5/MA10均線出場旗標、原本的停損降級成參考線——這種取代要在commit訊息或文件裡講清楚「這是延續還是取代了誰的哪個結論、為什麼」，不要默默蓋過去，讓使用者或之後接手的一方看得出前因後果。
6. **重大改動盡量留一個清楚的commit訊息說明背景**（哪怕不是每次都能走完整PR流程），方便對方或使用者事後用 `git log` 追蹤，不要只寫「fix」「update」這種看不出內容的訊息。

### 分工依專長，不是形式上分誰能動哪個檔

使用者要求分工要分到「自己擅長的」。這裡的「擅長」不是憑空猜的人設，是這個repo上實測出來的工具鏈可靠度差異：

- **Claude**：有穩定的本機 clone、完整 git/PR/GitHub Pages 部署流程，這個session也建立了一套自我糾錯過的實證回測方法論（隨機抽樣、out-of-sample驗證、抓出並修正自己的R-multiple陷阱與timeout-censoring bias）。凡是「要真的push進main、要走完整PR流程、要多輪嚴謹驗證後才下結論、要動到安全性/XSS這類全codebase一致性的review」，優先Claude。
- **Grok**：09-17當天示範過能獨立產出一版完整回測（FinMind真實資料、2.7年區間）並提出具體規則異動，這部分產出算數。但同一天也出現兩個執行面問題：`grok/trade-levels-accuracy` 分支的push機制本身壞掉，三次後續commit都聲稱「已還原/完整版」，實際diff是空的或只剩 `SEE_LOCAL_FILE` 佔位字串；寫入 `tradeLevels.js` 時把Claude逐行的回測依據註解整段清空，只留一行docs連結，增加了之後接手的成本。這兩個都不是分析邏輯錯，是「真的把東西寫回git」這一步不夠穩。
- 所以實務分法：Grok適合先產出假說、回測方案、規則比較（這正是公約第2條「先docs不要直接push main」在講的），由Claude覆核、跑測試、實際寫入程式碼與push；不是把「真的動程式碼、真的push main」整段都交給目前工具鏈還不穩的一方做。這不是階級之分，只是把容易出錯的最後一步放在目前比較不會出錯的地方，降低像09-17那樣「commit訊息說寫入了、實際diff是空的」這類事故再發生的機率。工具鏈穩定度如果之後改變，這個分法要跟著重新檢視，不是固定不變的規則。
